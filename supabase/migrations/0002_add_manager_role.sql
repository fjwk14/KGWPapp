-- =============================================================
-- 0002: membership_role に 'manager'(マネージャー)を追加
--
-- ⚠️ Supabase SQL Editor での実行手順(重要):
--   PostgreSQLの制約で「enum値の追加」と「その値の使用」は
--   同一トランザクションにできません。必ず
--     1) このファイル(0002)を Run
--     2) その後で 0003_manager_permissions.sql を Run
--   の順に、別々に実行してください。
--
--   既に 0001 を適用済みの本番DBに対して追記適用するための差分です。
--   新規デプロイ(0001をこれから流す)では 0001 に既に含まれているため
--   0002・0003 の実行は不要です。
-- =============================================================

alter type public.membership_role add value if not exists 'manager';
